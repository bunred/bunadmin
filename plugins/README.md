# BunAdmin Plugin

## Structure

A new plugin's structure should look like this:

- /[team]-[group]
    - /schemas
        - /[name]
            - index.tsx
    - package.json

Example:
- /bunadmin-blog
    - /schemas
        - /post
            - index.tsx
        - /category
            - index.tsx
    - package.json